# EJECUCION

1. Instalar dependencias:
   pip install pandas jupyter
   (opcional) pip install mysql-connector-python

2. Abrir Jupyter: jupyter notebook
   Abrir un notebook en notebooks/ y ejecutar las celdas en orden.

3. O ejecutar todo: python main.py
